#ENUMERATE

for index, value in enumerate(['apple', 'banana', 'cherry']): # here we are using the enumerate function to get both the index and the value of each item in the list
    print(index, value)