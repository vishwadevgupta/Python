#TERNARY OPERATOR

is_raining = True

is_wet = "Yes" if is_raining else "No" # Here it will check if is_raining is True or False,
                                       # if True it will assign "Yes" to is_wet, otherwise it will assign "No"

print(is_wet)